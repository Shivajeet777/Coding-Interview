package com.ss;

import java.util.Arrays;
import java.util.List;
import java.util.Map;
import java.util.stream.Collectors;

public class Mains {

	public static void main(String[] args) 
	{
	/*	List<User> users = Arrays.asList(
	            new User("Ram", 25),
	            new User("Shyam", 30),
	            new User("Geeta", 27),
	            new User("Hari", 35),
	            new User("Sita", 22),
	            new User("Sita", 22)
	        );
		
		 Map<Integer, List<User>> usersByAge = users.stream()
	                .collect(Collectors.groupingBy(User::getAge));

	        // Step 5: Print result
	        usersByAge.forEach((age, userList) -> {
	            System.out.println("Age " + age + ": " + userList);
	        });*/
		
		
	/*	List<User> users = Arrays.asList(
				
				new User("Ram",25),
				new User("Geeta", 27),
	            new User("Hari", 35),
	            new User("Sita", 22),
	            new User("Sita", 22),
	            new User("Sita", 22)
				
				
				
				);	
		
		
		   Map<Integer,List<User>> agegroup = users.stream().collect(Collectors.groupingBy(User::getAge));
		   
		 agegroup.forEach((age,userLis) -> {
			   System.out.println(age+""+userLis);
		   });*/
		   
//		   for(Map.Entry<Integer, List<User>> list:agegroup.entrySet())
//		   {
//			   System.out.println(list);
//		   }
		
		
		
		
	int result=	(int) Math.sqrt(4);
	
	System.out.println(result);
		
		
		
		
		
		

	}

}
